<%= render 'shared/banner' %>

<div class="flex flex-col items-center">
  <h1 class="text-4xl font-bold my-4 underline">All our flats :</h1>
  <div class="flex flex-col gap-4 w-full md:flex-row md:flex-wrap items-center justify-center">
    <% @flats.each do |flat| %>
      <%= render 'flat_card', flat: %>
    <% end %>
  </div>
</div>
